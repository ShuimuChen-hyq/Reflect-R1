jtype: Flow
version: '1'
with:
  port: 52000
executors:
  - name: clip_t
    uses:
      jtype: CLIPEncoder
      with:
        name: google/siglip-so400m-patch14-384
      metas:
        py_modules:
          - clip_server.executors.clip_torch
    timeout_ready: 3000000
    replicas: 1
